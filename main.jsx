import React from "react";
import ReactDOM from "react-dom/client";
import AutoPriceCalculator from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AutoPriceCalculator />
  </React.StrictMode>
);
